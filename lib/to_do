--apop_data_split needs to be split.
--rewrite global discussion in c_crash.
--probit didn't return names, which is wrong.
--regression should return partitioned data matrix?
--tests that produce a single column should use the data->vector.
--spacing in screen output
--Start respecting text segment of apop_data
--apop_db_to_text should output an apop_names structure, and I need a category printer.
--And how about something to write apop_estimates to the db?

apop_regression.c: In function `apop_produce_dummies':
apop_regression.c:407: warning: cast from pointer to integer of
different size
apop_regression.c:407: warning: cast from pointer to integer of
different size

apop_output.c: In function `apop_plot_histogram':
apop_output.c:143: warning: int format, different type arg (arg 3)
apop_output.c: In function `print_core_m':
apop_output.c:207: warning: field width is not type int (arg 2)

	#The next line will only work if you happen to be BK.
	rsync -avPe ssh html/ $(SFSSH):ap/htdocs/doc/

--apop_yule_rank model
--take residuals out of apop_estimate; generate them later.


text:
--2SLS
--multilevel examples
