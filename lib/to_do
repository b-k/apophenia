--There do exist reasons to have multiple databases in play at once.

--Read in files with any delimiter, not just commas.

--Decide what to do with zeros in the likelihood estimations of
functions which don't admit zeros. I have a few "if (x>0)"s floating
around, which is just wrong.

--the network-oriented likelihood estimates should have an option to
begin with zero links instead of one.
